Contrast Finance v0.5.116 changed-only package

Install over v0.5-2.115 only.
Archive layout: one root folder `v0.5-2.116/`, then `app/`, `tests/`, README and other changed files.
No database migration.
