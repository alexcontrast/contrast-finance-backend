Contrast Finance v0.5.115 verification

After Railway deploy open:
/api/status

Expected JSON field:
"version": "0.5.115"

Then open an AVR signing link on an iPhone. The primary `Открыть eGov Mobile` button must first open a local URL ending in `/ios-open`, not App Store and not `mobileSign:`.

On the `/ios-open` page Safari should expose the eGov Mobile Smart App Banner at the top. If the application is installed, tap `OPEN / Открыть`. The page also keeps the exact official SIGEX HTTPS deeplink and QR of the same one-time session as fallbacks.
