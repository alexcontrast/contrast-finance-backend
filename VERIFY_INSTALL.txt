Contrast Finance v0.5.119 verification

After deploy open:
/api/status

Expected version:
"version": "0.5.119"

Kaspi receipt identity check:
- upload or refresh a Kaspi self-employed receipt that contains a 12-digit IIN but no FIO;
- if the IIN exists in Contrast Finance history, FIO must be restored locally;
- otherwise the backend may use the fixed public lookup providers and persist the returned FIO;
- if no provider has a usable result, FIO stays empty and remains manually editable.

No database migration is required.
