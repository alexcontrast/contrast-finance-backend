Contrast Finance v0.5.117 verification

After deploy open:
/api/status

Expected version:
"version": "0.5.117"

In Accounting the Status filter must contain:
- Все
- Без Акта
- Не подписано ИП
- Не подписано СЗ

When either party signs an AVR, its ИП/СЗ badge must turn green automatically within the polling interval without a full page reload.
