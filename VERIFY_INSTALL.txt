Contrast Finance v0.5.113 verification

After Railway deploy open:
/api/status

Expected JSON field:
"version": "0.5.113"

If it still shows an older version, the application files were not replaced.

Then open an AVR signing link on an iPhone and tap `Open eGov Mobile`. The installed application must open directly with the current signing request; Safari must not navigate to the App Store.
