Contrast Finance v0.5.110 verification

After Railway deploy open:
/api/status

Expected JSON field:
"version": "0.5.110"

If it still shows an older version, the application files were not replaced.

Then hard refresh and open Accounting. Missing IINs are restored from historical receipts only when the normalized full name has one unique known IIN.
