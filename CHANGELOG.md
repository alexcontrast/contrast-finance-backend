# Contrast Finance — v0.5.47

## Исправлено

- В карточке мероприятия в админке в таблице заявок столбец «КГД» по-прежнему показывает налоговый режим.
- Подсказка при наведении на значение в столбце «КГД» теперь показывает название юрлица, а не дублирует налоговый режим.
- Если название юрлица отсутствует, подсказка остаётся пустой.

## Изменённые файлы

- `app/web/app.js`
- `app/web/index.html`
