Contrast Finance Backend v0.35.88 — changed files only

Шлифовка админки: фильтры заявок и удаление технического текста.

Изменено:
1. Убран текст `Скелет вкладок v0.30` из шапки админки.

2. Во вкладках заявок убран текстовый поиск по заказчику.
   Он запускал поиск/перерисовку после ввода букв и мешал.

3. Вместо него добавлен выпадающий фильтр `Заказчик`:
   - варианты берутся из заявок текущего месяца
   - есть пункт `Все заказчики`

4. Добавлен выпадающий фильтр `Менеджер`:
   - варианты берутся из заявок/мероприятий текущего месяца
   - есть пункт `Все менеджеры`

5. Фильтры заявок теперь не вызывают загрузку dashboard с сервера.
   Они просто быстро перерисовывают текущую вкладку из уже загруженных данных.

6. При смене месяца фильтры заказчика/менеджера сбрасываются в `Все`.

Backend не менялся относительно v0.35.87.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
