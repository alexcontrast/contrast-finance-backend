Contrast Finance Backend v0.35.65 — changed files only

Маленькая правка модалки оплаты.

Изменено:
- поле `Сумма заявки` теперь изначально пустое
- остаток показывается как placeholder
- при вводе суммы автоматически ставится разделение по тысячам: `123 000`
- отправка заявки всё равно отправляет чистое число без пробелов
- кнопка `Проверить` для БИН теперь показывает loading:
  - блокируется
  - пишет `Проверяем…`
  - после проверки возвращается обратно
- app.js прошёл node --check

Backend не трогался.
Миграций нет.
