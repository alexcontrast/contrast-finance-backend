Contrast Finance Backend v0.36.05 — changed files only

Шлифовка кнопок действий в админской модалке мероприятия.

Изменено:
- кнопки `Принять` и `Деньги в кассе` выровнены по высоте с `Закрыть`;
- `Принять` сделана зелёной;
- `Деньги в кассе` сделана ярко-зелёной;
- добавлена серая кнопка `На доработку`;
- кнопка `На доработку` переводит мероприятие в статус `revision` / `На доработке`.

Backend изменён:
- app/api/routes/events.py
  - добавлен роут POST /events/{event_id}/revision

Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
