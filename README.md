Contrast Finance Backend v0.37.02 — changed files only

Шлифовка после разделения status и money_status.

Изменено:
1. Архив заявок:
   - заявка уходит в архив, если она `Оплачена` и `Деньги в кассе`;
   - отменённые заявки также остаются в архиве.

2. В таблицах заявок:
   - колонка `Действия` заменена на `Статус денег`;
   - рядом показываются `Статус оплаты` и `Статус денег`.

3. Во вкладках `Мероприятия` и `Архив мероприятий`:
   - добавлена отдельная колонка `Статус денег`;
   - рабочий статус и статус денег теперь видны отдельно.

Миграций нет.
Backend-структура не менялась относительно v0.37.01.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
