Contrast Finance Backend v0.36.09 — changed files only

Точечная визуальная правка статуса мероприятия `Деньги в кассе`.

Исправлено:
- миникарточка / карточка мероприятия со статусом `Деньги в кассе`
  теперь красится зелёным, как статус `Принято`, а не серым;
- статус `cash_received` добавлен в accepted-tone для карточек мероприятий.

Backend не менялся относительно v0.36.08.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
