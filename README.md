Contrast Finance Backend v0.35.27 — changed files only

Фикс визуала миникарточек и кнопки удаления.

Frontend:
- реальные миникарточки менеджера теперь получают классы статуса:
  - `status-draft`
  - `status-review`
- Черновик остаётся серым
- На проверке выделяется зелёным
- открытая миникарточка получает `is-open` и выделяется отдельным синим акцентом
- кнопка `Удалить` добавлена в верхний ряд действий карточки мероприятия:
  `Оплатить / Передать / Соавтор / Удалить`
- кнопка `Удалить` красная и находится крайней справа рядом с `Соавтор`
- подключён обработчик удаления мероприятия

Backend:
- без изменений относительно v0.35.26

index.html подключает app.js/styles.css с ?v=0.35.27

Миграций нет.
