Contrast Finance Backend v0.35.31 — changed files only

Доработка визуала статусов и readonly.

Frontend:
- нижние кнопки `Сохранить черновик` и `Отправить Саше` для мероприятия `На проверке` теперь не только disabled, но и визуально серые
- все миникарточки базово серые
- открытая миникарточка выделяется зелёной обводкой и лёгким свечением, без цветной заливки
- статусные бейджи:
  - Черновик — серый
  - На проверке — жёлтый
  - Принято / approved / completed / done / archive — зелёный
  - На доработке / revision — серый
- добавлен helper `eventStatusToneClass(status)`, чтобы цвет статуса не смешивался с цветом самой миникарточки
- app.js проверен через `node --check`

index.html подключает app.js/styles.css с ?v=0.35.31

Миграций нет.
