Contrast Finance Backend v0.35.23 — changed files only

Жёсткий фикс пера КГД:

Frontend:
- кнопка проверки КГД и кнопка пера теперь физически разные кнопки
- ✓ использует data-check-tax-item и запускает проверку
- ✎ использует data-unlock-tax-item и НЕ умеет запускать КГД
- перо только:
  - разблокирует БИН/ИИН
  - разблокирует способ оплаты
  - сбрасывает НДС
  - сбрасывает Вычеты
  - обновляет текущую строку без перерисовки всей карточки
- убраны остатки data-tax-action, из-за которых перо могло попадать в сценарий проверки

Backend:
- без изменений относительно v0.35.22

index.html подключает app.js/styles.css с ?v=0.35.23

Миграций нет.
