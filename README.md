Contrast Finance Backend v0.35.61 — changed files only

Правка модалки данных менеджера.

Изменено:
- в шапке профильной модалки теперь только:
  - `Данные менеджера`
  - кнопка `Закрыть`
- убран зелёный eyebrow `МЕРОПРИЯТИЕ` именно для модалки профиля
- убран дублирующий заголовок `Данные менеджера` внутри формы
- убран текст `Можно изменить имя, телефон, отдел и email.`
- телефон в форме отображается в формате:
  `+7 (702) 123 45 67`
- поле телефона автоматически форматируется при вводе и потере фокуса
- app.js прошёл node --check

Backend не трогался.
Миграций нет.
