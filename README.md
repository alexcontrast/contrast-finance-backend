Contrast Finance Backend v0.36.01 — changed files only

Шлифовка админской модалки и вкладки `Мероприятия`.

Исправлено:
1. В смете админской модалки после переноса колонки `Способ` восстановлена правильная заливка:
   - НДС — синий столбик;
   - Вычеты — фиолетовый столбик;
   - Способ — без этой заливки.

2. Верхняя карточка `Налоги <процент> к уплате` теперь показывает именно сумму к уплате:
   внутренние налоги минус вычеты.

3. Во вкладке `Мероприятия` поиск по заказчику заменён на выпадающий список,
   как во вкладке `Заявки`.
   Варианты берутся из заказчиков текущего месяца.

4. Таблица мероприятий ужата:
   - уменьшен шрифт;
   - уменьшены отступы;
   - длинные значения режутся многоточием;
   - статус не переносится;
   - столбец `Заявки` должен помещаться без горизонтального скролла.

Backend не менялся относительно v0.36.00.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
