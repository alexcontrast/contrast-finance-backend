Contrast Finance Backend v0.35.22 — changed files only

Правки без перезагрузки карточки:

Frontend:
- поле `Факт` снова форматируется с пробелами после выхода из поля
- перо в КГД больше не запускает проверку КГД
- перо только разблокирует БИН/ИИН и способ оплаты
- после проверки КГД обновляются только ячейки НДС/Вычеты/БИН/кнопка
- после `Сохранить черновик` карточка не должна перерисовываться
- кнопка сохранения только отправляет данные и показывает `Сохранено`

Backend:
- без изменений относительно v0.35.21

index.html подключает app.js/styles.css с ?v=0.35.22

Миграций нет.
