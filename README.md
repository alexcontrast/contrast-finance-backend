Contrast Finance Backend v0.37.12 — changed files only

Визуальный фикс модалки менеджера `Мои оплаты`.

Проблема:
- модалка `Мои оплаты` снова растягивалась по ширине;
- кнопка `Закрыть` съезжала из шапки.

Исправлено:
1. При открытии `Мои оплаты` модалке добавляется класс:
   manager-payments-modal

2. Для этой модалки задана отдельная ширина:
   width/max-width = 860px

3. Шапка модалки:
   - flex nowrap;
   - кнопка `Закрыть` закреплена справа;
   - длинный заголовок не выталкивает кнопку.

4. Таблица/список оплат:
   - не растягивает всё окно;
   - получает внутренний горизонтальный скролл при необходимости.

5. При открытии других модалок класс manager-payments-modal снимается.

Миграций нет.
Backend не менялся относительно v0.37.11.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
