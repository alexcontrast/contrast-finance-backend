Contrast Finance Backend v0.35.51 — changed files only

Визуальная правка миникарточек.

Что изменено:
- верхние бейджи `Бюджет` и `Доход` фиксированы в один ряд: 2 колонки
- нижние бейджи `Статус` и `Соавтор` фиксированы в один ряд: 2 колонки
- бейджи больше не переносятся на следующую строку из-за длинной суммы или длинного имени
- при нехватке места текст сжимается по шрифту и обрезается через ellipsis
- для `Соавтор` добавлен title с полным именем при наведении
- app.js прошёл node --check

Backend не трогался.
Миграций нет.
