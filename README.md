# Contrast Finance v0.5.59

Точечный патч передачи рассчитанного налога в карточку мероприятия.

## Исправлено

- Админская карточка мероприятия на «Упрощёнке» получает `taxes_net` с backend и показывает те же полные 5% от оборота, что и кабинет менеджера.
- Для ОУР в карточке сохраняется расчёт налога с учётом вычетов.
- Добавлена совместимость со старыми ответами API без `taxes_net`.
- Месячный итог налогов ИП Contrast Event и общая копилка вычетов не менялись.

## Деплой

Обычный деплой. Миграций базы данных нет. Уже существующие мероприятия пересчитаются автоматически при следующей загрузке.
