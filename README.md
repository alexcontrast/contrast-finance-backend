Contrast Finance Backend v0.35.29 — changed files only

Прямой фикс реальных мест рендера.

Frontend:
- исправлена реальная строка миникарточки в `renderManagerEventList`
- статус `На проверке` получает зелёное выделение прямо inline + class/status
- открытая миникарточка получает отдельное синее выделение прямо inline + `is-open`
- кнопка `Удалить` вставлена прямо в верхний ряд действий карточки мероприятия:
  `Оплатить / Передать / Соавтор / Удалить`
- для мероприятия `На проверке`:
  - карточка получает readonly-оформление
  - поля мероприятия серые
  - нижние кнопки `Сохранить черновик` и `Отправить Саше` disabled
  - кнопка `Удалить` видна, но disabled
  - верхние `Оплатить / Передать / Соавтор` остаются доступными

Backend:
- без изменений относительно v0.35.28

index.html подключает app.js/styles.css с ?v=0.35.29

Миграций нет.
