Contrast Finance Backend v0.35.20 — changed files only

Правки после КГД и сохранения черновика:

Frontend:
- после успешной КГД-проверки блокируется не только БИН/ИИН, но и поле `Оплата`
- при нажатии на перо разблокируются и БИН/ИИН, и способ оплаты
- после КГД больше не вызывается полная перезагрузка карточки мероприятия
- вместо этого перерисовывается только текущая карточка из локального черновика
- после `Сохранить черновик` убрана лишняя полная перезагрузка карточки
- новые позиции после первого сохранения получают реальный backend ID и остаются в локальной смете без мигания

Backend:
- без изменений относительно v0.35.19

index.html подключает app.js/styles.css с ?v=0.35.20

Миграций нет.
