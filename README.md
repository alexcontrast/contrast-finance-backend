Contrast Finance Backend v0.35.1 — changed files only

Правки кабинета менеджера:

- убран выбор месяца/года из левой колонки миникарточек
- выбор месяца/года остаётся только сверху страницы
- возле заголовка `Мои мероприятия` больше не выводится имя менеджера и отдел
- кнопка `Сменить PIN` переносится рядом с кнопкой `Выйти`
- поле `Банк+налоги, % для Упрощенки` неактивно по умолчанию
- поле `Банк+налоги` становится активным только при выборе типа расчёта `Упрощенка`
- в модалке создания убран старый заголовок `Задать планы`
- создание мероприятия дополнительно отправляет status = draft
- после создания месяц переключается на месяц даты мероприятия, чтобы карточка сразу появилась в списке

Важно:
- редактирование данных менеджера через перышко будет следующим патчем, там нужна отдельная backend-ручка обновления профиля

index.html подключает app.js/styles.css с ?v=0.35.1

Миграций нет.
