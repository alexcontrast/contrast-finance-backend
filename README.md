Contrast Finance Backend v0.35.69 — changed files only

Правка модалки оплаты: БИН и карта.

Изменено:
1. По счету:
   - если БИН уже проверен и закреплён в смете, кнопка `Проверить` неактивна
   - если по позиции уже есть активная заявка по счету, кнопка `Проверить` тоже неактивна
   - поле БИН также неактивно

2. На карту:
   - номер карты обязателен до отправки заявки
   - если 16 цифр нет, ошибка показывается сразу в модалке
   - заявка не начинает создаваться
   - номер карты визуально форматируется по 4 цифры:
     `1234 5678 9012 3456`
   - backend всё равно получает чистые 16 цифр без пробелов

Backend не менялся.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
