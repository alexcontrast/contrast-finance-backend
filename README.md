Contrast Finance Backend v0.35.73 — changed files only

Точечный фикс кнопки `Проверить` в модалке оплаты по счету.

Исправлено:
- если у позиции уже есть закреплённый БИН (`iin_bin_locked = true` и БИН заполнен),
  кнопка `Проверить` в модалке становится реально disabled
- кнопка получает отдельный серый стиль и визуально выглядит неактивной
- нажатие дополнительно заблокировано в обработчике
- БИН считается закреплённым даже если налоговый статус по какой-то причине не пришёл в модалку,
  но `iin_bin_locked = true`

Backend не менялся.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
