Contrast Finance Backend v0.37.05 — changed files only

Точечный визуальный фикс модалки мероприятия.

Исправлено:
- модалка мероприятия снова расширена до нормальной рабочей ширины;
- верхние финансовые карточки больше не сжимают цифры;
- таблица сметы и таблица заявок внутри модалки не растягивают всю модалку;
- длинные данные внутри таблиц аккуратно обрезаются/скроллятся по горизонтали внутри блока;
- правка не применяется к менеджерской модалке оплаты, PIN-модалке и профилю.

Миграций нет.
Backend не менялся относительно v0.37.04.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
