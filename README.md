Contrast Finance Backend v0.35.70 — changed files only

Точечный фикс блокировок в модалке оплаты и смете.

Исправлено:
1. Самозанятый, созданный в смете без фамилии:
   - поле `Фамилия самозанятого` в модалке оплаты теперь активно
   - поле блокируется только если фамилия уже есть или есть активная заявка с фамилией
   - заявка снова может быть создана после ввода фамилии

2. По счету после проверки БИН в смете:
   - если БИН проверен и зафиксирован (`iin_bin_locked = true`), способ оплаты в смете теперь тоже блокируется
   - раньше блокировался только сам БИН, а способ оплаты можно было менять

3. В модалке заявки:
   - если БИН уже проверен в смете или есть активная заявка по счету, кнопка `Проверить` остаётся неактивной
   - поле БИН тоже остаётся неактивным

Backend не менялся.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
