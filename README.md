Contrast Finance Backend v0.35.26 — changed files only

Аварийный фикс входа.

Проблема:
- в app.js после прошлого патча оказался повторный блок `loadingCounter / setLoading / withLoading`
- из-за этого весь frontend-скрипт падал при загрузке, поэтому кнопка `Вход` не реагировала

Исправлено:
- убран повторный блок loading helper'ов
- app.js проверен через `node --check`
- кнопка `Вход` теперь показывает лёгкую загрузку `Входим…` и временно блокируется
- если вход не удался, кнопка возвращается в обычное состояние

Backend:
- без изменений относительно v0.35.25

index.html подключает app.js/styles.css с ?v=0.35.26

Миграций нет.
