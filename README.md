Contrast Finance Backend v0.36.06 — changed files only

Шлифовка статусов и кнопок в админской модалке мероприятия.

Изменено:
1. Бейдж статуса `Принято` теперь зелёный.

2. Бейдж статуса `Деньги в кассе` теперь ярко-зелёный,
   в цвет кнопки `Деньги в кассе`.

3. У принятого мероприятия кнопка `Принять` больше не исчезает:
   - она остаётся видимой;
   - становится неактивной визуально и функционально.

4. У мероприятий `Черновик` и `На доработке` кнопка `Удалить` всегда видна:
   - если активных оплат нет — активна;
   - если активные оплаты есть — неактивна визуально и функционально.

Backend не менялся относительно v0.36.05.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
