Contrast Finance Backend v0.35.52 — changed files only

Фикс логики дохода при соавторстве.

Точная причина:
- в предыдущем патче деление на `share_percent` было добавлено прямо в общий `calculateDraftSummaryPreview()`
- эта функция используется и в миникарточке, и в основной карточке мероприятия
- поэтому доход в основной карточке тоже делился на 2, хотя там должен отображаться полный доход мероприятия

Исправлено:
- `calculateDraftSummaryPreview()` снова возвращает полный расчёт мероприятия, 100%
- основная карточка получает полный доход и полную ЗП/прибыль мероприятия
- деление по `share_percent` применяется только в `updateCurrentManagerMiniCardLive()`
  для миникарточки и manager-dashboard строки
- миникарточка продолжает показывать доход менеджера по доле, 50%
- app.js прошёл node --check

Backend не трогался.
Миграций нет.
