Contrast Finance Backend v0.35.30 — changed files only

Причина, почему визуальные правки не применялись:
- в app.js было две копии большого frontend-кода
- первая копия была новая, с кнопкой `Удалить`, readonly и цветами миникарточек
- ниже после первого `boot()` начиналась старая дублированная копия функций
- JavaScript использовал последние объявленные функции, поэтому старая копия перезаписывала новые правки

Исправлено:
- удалён дублированный старый хвост app.js после первого `boot()`
- теперь в app.js осталась одна актуальная копия функций
- проверено:
  - renderManagerEventCard: 1 раз
  - renderManagerEventList: 1 раз
  - renderManagerDashboard: 1 раз
  - api: 1 раз
- app.js проверен через `node --check`

index.html подключает app.js/styles.css с ?v=0.35.30

Миграций нет.
