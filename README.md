Contrast Finance Backend v0.35.28 — changed files only

Визуальный readonly для мероприятий `На проверке`.

Frontend:
- мероприятие со статусом `На проверке` визуально становится недоступным для редактирования
- поля мероприятия, смета и таблицы становятся серыми
- появляется плашка:
  `Мероприятие на проверке — смета и поля недоступны для редактирования`
- нижние кнопки `Сохранить черновик` и `Отправить Саше` становятся неактивными
- кнопка `Удалить` остаётся видимой, но неактивной
- верхние действия остаются доступными:
  `Оплатить`, `Передать`, `Соавтор`

Backend:
- без изменений относительно v0.35.27

index.html подключает app.js/styles.css с ?v=0.35.28

Миграций нет.
