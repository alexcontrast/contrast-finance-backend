Contrast Finance Backend v0.35.24 — changed files only

Фикс кнопки галочки КГД.

Проблема:
- после разделения кнопок ✓ и ✎ обработчик клика для новой галочки не привязывался стабильно

Исправлено:
- добавлена отдельная функция bindTaxButtons()
- ✓ стабильно вызывает checkTaxForItem()
- ✎ стабильно вызывает unlockTaxForItem()
- после замены кнопки в строке обработчики привязываются заново
- перо по-прежнему не запускает КГД, только разблокирует и сбрасывает НДС/Вычеты

Backend:
- без изменений относительно v0.35.23

index.html подключает app.js/styles.css с ?v=0.35.24

Миграций нет.
