Contrast Finance Backend v0.35.87 — changed files only

Фикс админки после общей доработки кнопок и менеджерского месяца.

Исправлено:
1. В админке скрыт зелёный бейдж пользователя `Admin / Админ` рядом со `Сменить PIN`.
   Для админа в верхней панели остаются кнопки управления без лишней зелёной плашки.

2. Переключение месяца/года в админке:
   - запрос `/admin-dashboard` теперь получает cache-buster
   - если backend вернул ошибку, старый экран больше не остаётся висеть
   - показывается пустой dashboard выбранного месяца
   - frontend дополнительно фильтрует события по выбранному месяцу
   - если backend случайно вернул лишние события, они не попадут в текущий месяц

3. Backend admin-dashboard больше не падает, если на выбранный месяц ещё не задан план:
   - план считается 0
   - админка всё равно открывает выбранный месяц

Изменён backend:
- app/api/routes/admin_dashboard.py

Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
