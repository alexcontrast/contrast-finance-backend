Contrast Finance Backend v0.35.81 — changed files only

Визуальная правка статусов и модалки `Мои оплаты`.

Изменено:
1. Статус заявки `Новая` теперь серый бейдж по всей системе:
   - в менеджерских оплатах
   - в админских таблицах оплат
   - во всех местах, где используется `.status.new`

2. Модалка `Мои оплаты` стала ещё уже:
   - сжата сама белая модалка, а не только строки внутри
   - убрано лишнее боковое пространство от шапки и кнопки `Закрыть`
   - список оплат занимает ширину по содержимому
   - строки оплат стали ещё плотнее по ширине

Backend не менялся относительно v0.35.80.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
