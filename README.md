Contrast Finance Backend v0.36.03 — changed files only

Последняя мелкая шлифовка вкладки `Админка → Мероприятия`.

Изменено:
- в шапке таблицы слово `Заявки` теперь помещается полностью;
- столбец `Заявки` слегка расширен;
- денежные столбцы немного ужаты, чтобы таблица не расползалась.

Backend не менялся относительно v0.36.02.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
