Contrast Finance Backend v0.35.78 — changed files only

Оптимизация модалки `Мои оплаты`.

Изменено:
- убрано повторение названия позиции внутри каждой заявки
- позиция показывается один раз как компактный заголовок группы
- внутри группы каждая заявка теперь одной низкой строкой:
  - Сумма
  - Способ
  - Статус
  - Действие
- убрана отдельная шапка колонок внутри каждой группы
- строки стали ниже, плотнее и без лишнего воздуха
- сумма заявки осталась жирной и заметной
- кнопка `Отменить` остаётся неактивной для оплаченных/закрытых заявок

Backend не менялся относительно v0.35.77.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
