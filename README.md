Contrast Finance Backend v0.35.71 — changed files only

Важное правило защиты сметы: позицию нельзя удалить, если по ней есть активная заявка на оплату.

Исправлено:
- если по позиции есть хотя бы одна активная заявка на оплату, кнопка удаления строки `×` становится неактивной
- активная заявка = любой статус, кроме `rejected`
- если все заявки по позиции отменены, кнопка удаления снова станет активной после обновления данных
- добавлена дополнительная защита в самой функции удаления:
  даже если кнопку каким-то образом нажать, позиция не удалится и покажется ошибка
- правило работает и во внешней, и во внутренней смете

Backend не менялся.
Миграций нет.

Проверки:
- app.js прошёл node --check
- Python-файлы компилируются
