Contrast Finance Backend v0.35.62 — changed files only

Фикс ширины полей в модалке данных менеджера.

Точная причина:
- модалка профиля стала компактной, но внутренняя form-grid всё ещё могла раскладывать поля в несколько колонок
- из-за этого поля стали узкими
- глобальный крупный шрифт input дополнительно обрезал имя/email/телефон

Исправлено:
- профильная модалка принудительно переведена в одну колонку
- каждое поле занимает 100% ширины модалки
- шрифт внутри полей уменьшен только для profile-modal-mode
- телефонное поле чуть дополнительно ужато по letter-spacing/font-size
- формат телефона `+7 (702) 123 45 67` сохранён
- app.js прошёл node --check

Backend не трогался.
Миграций нет.
